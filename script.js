let num1;
let num2;
let operator;

// Need add, subtract, multiply, and divide operations

// Create a new function operate that takes an operator and 2 numbers and then calls one of the above functions(add, subtract, multiply, divide) on the numbers.